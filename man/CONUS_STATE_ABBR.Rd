% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geography.R
\name{CONUS_STATE_ABBR}
\alias{CONUS_STATE_ABBR}
\title{Contiguous-US state USPS abbreviations (48 states + DC), DERIVED from FIPS}
\format{
Character vector of 49 USPS codes.
}
\source{
Derived from \link{CONUS_STATE_FIPS} via the U.S. Census Bureau ANSI/FIPS
crosswalk, \url{https://www.census.gov/library/reference/code-lists/ansi.html}.
}
\usage{
CONUS_STATE_ABBR
}
\description{
The abbreviation representation of \link{CONUS_STATE_FIPS}. Derived through the
canonical FIPS->USPS crosswalk so the FIPS and abbreviation forms of "CONUS"
can never drift apart. Use this instead of re-deriving
\code{setdiff(c(state.abb, "DC"), c("AK", "HI"))} inline. NOTE: plain
\code{c(state.abb, "DC")} (all 50 + DC, includes AK/HI) is a different set -- a
state-validation vocabulary, NOT CONUS.
}
\examples{
length(CONUS_STATE_ABBR) # 49
any(c("AK", "HI") \%in\% CONUS_STATE_ABBR) # FALSE (excluded by construction)
}
\seealso{
\link{CONUS_STATE_FIPS} (the FIPS form it is derived from),
\link{NON_CONTIGUOUS_CODES}

Other geography constants:
\code{\link{CONUS_STATE_FIPS}},
\code{\link{NON_CONTIGUOUS_CODES}},
\code{\link{NON_CONTIGUOUS_FIPS}}
}
\concept{geography constants}
