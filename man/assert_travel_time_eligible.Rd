% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinate_fitness.R
\name{assert_travel_time_eligible}
\alias{assert_travel_time_eligible}
\title{Refuse coordinates that are not fit for routing}
\usage{
assert_travel_time_eligible(df, context = "travel-time analysis")
}
\arguments{
\item{df}{\code{data.frame}/\code{tibble}/\code{sf}: rows to check. May carry
\code{usable_for_travel_time} (logical) and/or \code{coord_source} (character).}

\item{context}{\code{character(1)}: label for the error message, e.g. the calling
function. Default "travel-time analysis".}
}
\value{
Invisibly \code{TRUE} when every row is fit; otherwise \code{stop()}s with a
count of the offending rows and how to exclude them.
}
\description{
Call at the top of any function that generates isochrones, computes drive
times, or otherwise treats a coordinate as a real practice location.
}
\details{
Two independent signals are checked, either of which is disqualifying:
\itemize{
\item a logical \code{usable_for_travel_time} column that is \code{FALSE}, or \code{NA};
\item a \code{coord_source} value containing "centroid" (city, county or ZIP
centroid geocodes).
}
A data frame carrying neither column passes, so this is safe to call on
inputs that predate the convention -- it constrains what it can see and does
not invent a verdict about what it cannot.
}
\section{Unknown fitness is not eligible}{

\code{usable_for_travel_time} is a three-state contract, not a boolean:
\itemize{
\item \code{TRUE} -- eligible.
\item \code{FALSE} -- refused: the coordinate is known to be unfit.
\item \code{NA} -- refused, with a \emph{distinct} message: fitness was never
established.
}
\code{NA} is refused because "eligible unless proven otherwise" is the wrong
default for a guard protecting travel-time inference -- an unverified
coordinate that silently passes is the failure this function exists to
prevent. It is refused \emph{separately} from \code{FALSE} because "we never checked
this row" and "this row is known bad" are different facts, and a caller
reporting an exclusion needs to say which. Do not coerce \code{NA} to \code{FALSE}
upstream to satisfy this: that discards the provenance of never having
checked.
}

\examples{
\dontrun{
generate_isochrones <- function(points) {
  assert_travel_time_eligible(points, context = "generate_isochrones()")
  # ...
}

# Deliberate exclusion, which must then be reported:
routable <- subset(providers, !grepl("centroid", coord_source))
message(nrow(providers) - nrow(routable), " excluded: centroid geocodes")
}
}
\seealso{
Other coordinate-fitness:
\code{\link{coordinate_fitness}}
}
\concept{coordinate-fitness}
