% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workforce_statistics.R
\name{calculate_replacement_gap}
\alias{calculate_replacement_gap}
\title{Subspecialty replacement-gap analysis (retirees vs fellowship graduates)}
\usage{
calculate_replacement_gap(
  retirees_by_subspec,
  fellowship_grads,
  horizon_years = 5
)
}
\arguments{
\item{retirees_by_subspec}{Data frame: \code{subspecialty}, \code{retiring_count}.}

\item{fellowship_grads}{Data frame: \code{subspecialty}, \code{graduates}, one row per
observed year.}

\item{horizon_years}{Projection horizon in years (default 5).}
}
\value{
List: \code{by_subspecialty} (data frame, ordered by subspecialty) and
\code{overall} (summary list).
}
\description{
Projects \code{horizon_years} of graduates against projected retirements per
subspecialty and reports the net gap, the replacement ratio, and an adequacy
flag.
}
\details{
\code{horizon_years} is a parameter, not a literal. The versions this replaces
multiplied by a hardcoded 5 next to a comment explaining that 5 was
"horizon - reference", so moving the horizon silently left the arithmetic
behind.

A subspecialty with no matching graduate rows contributes 0 graduates, not
NA: absence of a fellowship is a real zero. A subspecialty with zero
retirements yields an NA replacement ratio rather than Inf, because "nobody
retiring" has no ratio to report.
}
\examples{
calculate_replacement_gap(
  data.frame(subspecialty = c("FPMRS", "GO"), retiring_count = c(50, 30)),
  data.frame(subspecialty = c("FPMRS", "FPMRS", "GO"), graduates = c(10, 12, 4))
)$overall$replacement_ratio
}
