% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/npi.R
\name{canon_npi}
\alias{canon_npi}
\title{Canonicalize NPI identifiers to 10-digit strings (SSOT)}
\usage{
canon_npi(x, verbose = TRUE)
}
\arguments{
\item{x}{Atomic vector of raw NPI values (character or numeric).}

\item{verbose}{Logical; when \code{TRUE} (default) emit a per-reason summary of
rejected values via \code{\link[=message]{message()}}.}
}
\value{
Character vector the same length as \code{x}: a 10-digit NPI or
\code{NA_character_}.
}
\description{
The single canonical NPI normalizer for the OB/GYN subspecialty projects.
Promoted verbatim from isochrones (\code{R/join_standards.R}) so \code{twostep}, \code{cliff},
\code{simulation}, \code{mysterycall}, and \code{mysterymaps} stop re-rolling weaker copies
(\code{sprintf("\%.0f")}, \code{as.integer()}, \code{trimws()}), which silently corrupt NPIs
via leading-zero loss or 32-bit overflow.
}
\details{
Strips whitespace/\code{-}/\code{.} separators; rejects scientific notation, embedded
letters, non-digit-only values, and values with more than 10 digits; then
left-zero-pads to 10 and validates \verb{^[0-9]\{10\}$}. Rejected and NA/empty inputs
return \code{NA_character_}.
}
\examples{
canon_npi(c("1234567893", "12-3456 7890", "abc", NA), verbose = FALSE)
}
