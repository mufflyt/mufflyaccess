% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessibility_stats.R
\name{mc_weighted_ci}
\alias{mc_weighted_ci}
\title{Monte-Carlo CI for a population-weighted accessibility statistic.
Redraws weights ~ Normal(est, se) B times (unbiased: point estimate lies inside its interval).}
\usage{
mc_weighted_ci(
  access,
  est,
  se,
  stat = c("mean", "zero"),
  B = 2000L,
  probs = c(0.025, 0.975),
  seed = 1L
)
}
\arguments{
\item{access}{numeric accessibility values.}

\item{est}{numeric weight estimates.}

\item{se}{numeric weight standard errors (ACS MOE / z90).}

\item{stat}{"mean"/"zero".}

\item{B}{draws.}

\item{probs}{interval quantiles.}

\item{seed}{RNG seed.}
}
\value{
named numeric c(point, lo, hi).
}
\description{
Monte-Carlo CI for a population-weighted accessibility statistic.
Redraws weights ~ Normal(est, se) B times (unbiased: point estimate lies inside its interval).
}
\examples{
# with zero standard errors the interval collapses to the point estimate
mc_weighted_ci(c(1, 3), est = c(1, 3), se = c(0, 0), B = 100)
# -> c(point = 2.5, lo = 2.5, hi = 2.5)
\dontrun{
# real use: ACS estimates with their MOE-derived standard errors
mc_weighted_ci(access, est = pop_est, se = pop_moe / ACS_MOE_Z90)
}
}
\seealso{
\code{\link[=weighted_mean_all]{weighted_mean_all()}}, \code{\link[=zero_access_share]{zero_access_share()}}

Other accessibility-disparity statistics:
\code{\link[=acs_year_of]{acs_year_of()}},
\code{\link[=annual_trend]{annual_trend()}},
\code{\link[=rurality_from_ruca]{rurality_from_ruca()}},
\code{\link[=tract_vintage_of]{tract_vintage_of()}},
\code{\link[=weighted_mean_all]{weighted_mean_all()}},
\code{\link[=zero_access_share]{zero_access_share()}}
}
\concept{accessibility-disparity statistics}
