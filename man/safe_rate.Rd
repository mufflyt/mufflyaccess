% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/safe_divide.R
\name{safe_rate}
\alias{safe_rate}
\title{Safe Rate Calculation (per N)}
\usage{
safe_rate(events, exposure, multiplier = 1, digits = 1, default = NA_real_)
}
\arguments{
\item{events}{\code{numeric}: event count (numerator).}

\item{exposure}{\code{numeric}: population at risk (denominator); zero/NA yields
\code{default}.}

\item{multiplier}{\verb{numeric scalar}: rate base, e.g. \code{1e5} for per-100,000
(default 1).}

\item{digits}{\code{integer}: rounding digits (default 1).}

\item{default}{\verb{numeric scalar}: value on a zero/NA exposure (default \code{NA_real_}).}
}
\value{
\code{numeric} rate per \code{multiplier}, rounded to \code{digits}.
}
\description{
Epidemiological rate \code{(events / exposure) * multiplier}, rounded,
with a zero/NA-exposure guard. Returns \code{NA_real_} (not 0) so sparse tracts
are distinguishable from true zero-rate tracts.
}
\examples{
safe_rate(890, 164690617, multiplier = 1e5)  # ~0.5 gyn-onc per 100,000 women
safe_rate(5, 0, multiplier = 1e5)             # NA_real_
}
\seealso{
\code{\link[=safe_divide]{safe_divide()}}, \code{\link[=safe_ratio]{safe_ratio()}}

Other safe-arithmetic:
\code{\link[=safe_divide]{safe_divide()}},
\code{\link[=safe_divide_manu]{safe_divide_manu()}},
\code{\link[=safe_pct_manu]{safe_pct_manu()}},
\code{\link[=safe_percent]{safe_percent()}},
\code{\link[=safe_ratio]{safe_ratio()}}
}
\concept{safe-arithmetic}
