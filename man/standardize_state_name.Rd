% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state.R
\name{standardize_state_name}
\alias{standardize_state_name}
\title{Standardize US state values to canonical names or 2-letter codes (SSOT)}
\usage{
standardize_state_name(x, output = c("name", "abbr"))
}
\arguments{
\item{x}{Character vector of raw state values (full names, codes, or
underscored names, any case).}

\item{output}{\code{"name"} (default) for the full name, or \code{"abbr"} for the
2-letter USPS code.}
}
\value{
Character vector: canonical name or code; unmapped values fall back to
title case (for \code{"name"}) or \code{NA_character_} (for \code{"abbr"}).
}
\description{
The single canonical state-name normalizer for the OB/GYN subspecialty
projects. Promoted from isochrones (\code{R/utils_standardized.R}) so consumers stop
re-rolling \code{setNames(state.abb, state.name)} lookups. Handles the 50 states, DC,
and the five territories (PR, GU, VI, AS, MP) in either direction.
}
\details{
The output is byte-identical to the isochrones original; only two isochrones-
specific SIDE EFFECTS were dropped (an unconditional \code{[INFO]} print and an
audible \code{beep}), which have no place in a shared library and do not affect the
returned value.
}
\examples{
standardize_state_name(c("colorado", "TX", "Puerto Rico"), output = "abbr")
standardize_state_name(c("co", "tx"), output = "name")
}
