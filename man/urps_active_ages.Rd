% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_active_ages.R
\name{urps_active_ages}
\alias{urps_active_ages}
\title{The URPS active-age distribution}
\usage{
urps_active_ages(
  pathway = "ABOG_PLUS_ABU",
  geography = "national",
  as = c("counts", "vector")
)
}
\arguments{
\item{pathway}{Board pathway: \code{"ABOG_PLUS_ABU"} (default), \code{"ABOG"}, or
\code{"ABU_NET_NEW"}. Case-insensitive.}

\item{geography}{\code{"national"} (default) or \code{"conus"}.}

\item{as}{\code{"counts"} (default) for one row per age, or \code{"vector"} for the
distribution expanded to one element per provider -- the form a
microsimulation consumes directly.}
}
\value{
With \code{as = "counts"}, a \code{data.frame} with columns \code{age} (integer) and
\code{n_active} (integer), ordered by age. With \code{as = "vector"}, an integer
vector of length equal to the active count.
}
\description{
Age distribution of the active urogynecology cohort, served from
the bundled artifact so no consumer has to carry its own copy. This is the
input an age-structured projection starts from; pair it with
\code{\link[=urps_retirement_hazard]{urps_retirement_hazard()}} to run one.
}
\details{
The stored artifact holds the two base pathways (\code{ABOG},
\code{ABU_NET_NEW}) for each geography. \code{ABOG_PLUS_ABU} is summed on read rather
than stored, so a combined total cannot disagree with its parts.

The result is checked against \code{\link[=urps_count]{urps_count()}} on every call: the ages must
total the published active count for the same pathway and geography. A
distribution that does not add up to the count the package serves is a
contradiction, and it fails loud rather than returning a plausible number.
}
\examples{
head(urps_active_ages())
length(urps_active_ages(as = "vector"))     # == urps_count(2023, "board_certified_active",
#                                                "national", include_urology = TRUE)
head(urps_active_ages("ABOG", "conus"))
}
\seealso{
\code{\link[=urps_count]{urps_count()}} for the totals these must reconcile to,
\code{\link[=urps_projection]{urps_projection()}} for the canonical projection built from them,
\code{\link[=urps_retirement_hazard]{urps_retirement_hazard()}}, \code{\link[=urps_lineage]{urps_lineage()}}.

Other URPS SSOT:
\code{\link[=urps_projection]{urps_projection()}}
}
\concept{URPS SSOT}
