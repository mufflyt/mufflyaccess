% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_lfp.R
\name{urps_apply_lfp}
\alias{urps_apply_lfp}
\title{Apply LFP to a certified cohort to obtain the practicing headcount}
\usage{
urps_apply_lfp(cohort)
}
\arguments{
\item{cohort}{A \code{data.frame} with columns \code{age} (numeric/integer), \code{sex}
(\code{"female"} / \code{"male"}), and \code{certified_n} (numeric). Other columns are
passed through unchanged.}
}
\value{
The same \code{data.frame} with a new column \code{practicing_n} =
\code{certified_n * urps_p_active(age, sex)}. Any existing \code{practicing_n}
column is overwritten.
}
\description{
Multiplies \code{certified_n} by \code{urps_p_active(age, sex)} to produce
\code{practicing_n} — the expected number of providers actively practicing in a
given year conditional on still holding certification. This is the first step
of the supply pipeline before applying the FTE weight:

\if{html}{\out{<div class="sourceCode">}}\preformatted{cohort <- urps_apply_lfp(cohort)   # certified_n -> practicing_n
fte    <- urps_effective_fte_sex(   # practicing_n -> supply_clinical_fte
            data.frame(..., n = cohort$practicing_n), scale)
}\if{html}{\out{</div>}}

Or call \code{\link[=urps_supply_fte_sex]{urps_supply_fte_sex()}} to do both steps in one call.
}
\examples{
cohort <- data.frame(
  age = c(45L, 62L, 45L, 62L),
  sex = c("female", "female", "male", "male"),
  pathway = c("ABOG", "ABOG", "ABU", "ABU"),
  certified_n = c(350, 150, 100, 40))
urps_apply_lfp(cohort)
}
\seealso{
\code{\link[=urps_p_active]{urps_p_active()}}, \code{\link[=urps_supply_fte_sex]{urps_supply_fte_sex()}}, \code{\link[=urps_effective_fte_sex]{urps_effective_fte_sex()}}

Other URPS LFP:
\code{\link{URPS_LFP_VERSION}},
\code{\link[=urps_lfp_curve]{urps_lfp_curve()}},
\code{\link[=urps_lfp_params]{urps_lfp_params()}},
\code{\link[=urps_p_active]{urps_p_active()}}
}
\concept{URPS LFP}
