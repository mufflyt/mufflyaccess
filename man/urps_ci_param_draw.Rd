% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_projection_ci.R
\name{urps_ci_param_draw}
\alias{urps_ci_param_draw}
\title{Draw one perturbed parameter set for a bootstrap replicate}
\usage{
urps_ci_param_draw(retirement_sigma_sd = 0.51, entrant_cv = 0.077, seed = NULL)
}
\arguments{
\item{retirement_sigma_sd}{Standard deviation of the retirement-curve shift in
years. Default 0.51 gives ±1 year at the 95\% level (1.96 × 0.51 ≈ 1).}

\item{entrant_cv}{Coefficient of variation for the entrant scale factor.
Default 0.077 gives ±15\% at the 95\% level (1.96 × 0.077 ≈ 0.15).}

\item{seed}{Single integer RNG seed, or \code{NULL} (default) to use the current
RNG state.}
}
\value{
A named list with elements:
\describe{
\item{retirement_sigma_sd}{the \code{retirement_sigma_sd} argument, passed
through for traceability}
\item{retirement_shift}{\code{rnorm(1, 0, retirement_sigma_sd)} — additive
year shift applied to the retirement-curve mu}
\item{entrant_scale}{\code{rnorm(1, 1, entrant_cv)} — multiplicative scale
applied to n_entrants}
\item{lfp_intercept_shift}{\code{rnorm(1, 0, 0.05)} — additive shift to the
LFP logistic intercept}
}
}
\description{
Samples one set of perturbation parameters from their marginal
distributions for a single parametric-bootstrap replicate. The returned list
is passed as the \code{param_draw} argument to the user-supplied \code{project_fn} in
\code{\link[=urps_projection_ci]{urps_projection_ci()}}.
}
\examples{
urps_ci_param_draw(seed = 1L)
# reproducible draw
a <- urps_ci_param_draw(seed = 42L)
b <- urps_ci_param_draw(seed = 42L)
identical(a, b) # TRUE
}
\seealso{
\code{\link[=urps_projection_ci]{urps_projection_ci()}}, \code{\link[=urps_retirement_params]{urps_retirement_params()}},
\code{\link[=urps_lfp_params]{urps_lfp_params()}}

Other URPS projection CI:
\code{\link[=urps_projection_ci]{urps_projection_ci()}}
}
\concept{URPS projection CI}
