% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_demand.R
\name{urps_demand_clinical_fte}
\alias{urps_demand_clinical_fte}
\title{Compute demand_clinical_fte from a patient population and the fitted equations}
\usage{
urps_demand_clinical_fte(
  population,
  visits_per_fte,
  obesity_prev_shift = 0,
  insurance_expansion_factor = 1,
  managed_care_factor = 1,
  retail_clinic_share = 0
)
}
\arguments{
\item{population}{A \code{data.frame} with columns matching the \verb{b_*} covariate
names in \code{\link[=urps_demand_params]{urps_demand_params()}} (age, sex, race, bmi, etc.) and a column
\code{n} (population count per row).}

\item{visits_per_fte}{Annual URPS visits per full-time-equivalent provider
(visits -> FTE conversion denominator). No default; must be supplied.}

\item{obesity_prev_shift}{See \code{\link[=urps_demand_levers]{urps_demand_levers()}}. Default \code{0}.}

\item{insurance_expansion_factor}{See \code{\link[=urps_demand_levers]{urps_demand_levers()}}. Default \code{1}.}

\item{managed_care_factor}{Multiplier on total physician demand from HMO/ACO
gatekeeping. Default \code{1} (no change). See \code{\link[=urps_demand_levers]{urps_demand_levers()}}.}

\item{retail_clinic_share}{Fraction of office-visit demand shifted to retail
clinics, reducing URPS physician demand. Must be in [0, 1). Default \code{0}.
See \code{\link[=urps_demand_levers]{urps_demand_levers()}}.}
}
\value{
Length-1 numeric \code{demand_clinical_fte} when the demand model is
calibrated; \code{NA_real_} while it is the uncalibrated skeleton. \code{population}
is a design-matrix \code{data.frame}: an \code{n} count column plus covariate columns
named as the fit's design terms (\code{age}, \code{sex_male}, \code{race_black}, \code{bmi},
...); an absent covariate is taken at its reference level (\code{0}).
}
\description{
Predicted ambulatory-visit demand for a patient population,
converted to clinical FTE. \strong{Returns \code{NA_real_} while the demand model is
uncalibrated} (the \code{urps_demand_params()} NA skeleton). Once a fitted
artifact is active -- \code{options(mufflyaccess.urps_demand_params_path = ...)} or
\code{MUFFLYACCESS_URPS_DEMAND_PARAMS}, see \code{\link[=read_urps_demand_params]{read_urps_demand_params()}} -- it
evaluates the visit-count regressions per person, sums over the population,
applies the scenario demand levers, and divides by \code{visits_per_fte}.
}
\details{
The pipeline, evaluated once \code{urps_demand_params()} is calibrated
(office + outpatient visit-count services drive office-based URPS FTE):
\enumerate{
\item For each service type, evaluate the regression at each person's covariate
vector to get predicted visit rate.
\item Multiply by person count -> expected visits.
\item Apply demand scenario levers:
\itemize{
\item \code{obesity_prev_shift}: adjusts PFD-relevant covariate distribution.
\item \code{insurance_expansion_factor}: scales visits from newly insured persons.
\item \code{managed_care_factor}: multiplies total specialist visit demand.
\item \code{retail_clinic_share}: reduces physician demand by the share of visits
that shift to retail clinics (\code{visits_physician = visits_total * (1 - share)}).
}
\item Multiply by specialty x setting calibration scalar (\code{urps_demand_scalars()}).
\item Divide by \code{visits_per_fte} -> \code{demand_clinical_fte}.
}

Use \code{\link[=urps_demand_levers]{urps_demand_levers()}} to retrieve a scenario's lever bundle from the
registry rather than passing raw numeric values.
}
\examples{
levers <- urps_demand_levers("demand_managed_care_increase")
urps_demand_clinical_fte(
  population = data.frame(age = 50, sex = "female", n = 1000),
  visits_per_fte = 2000,
  managed_care_factor = levers$demand_managed_care_factor,
  retail_clinic_share = levers$demand_retail_clinic_share
) # NA_real_ until calibrated
}
\seealso{
\code{\link[=urps_demand_params]{urps_demand_params()}}, \code{\link[=urps_demand_scalars]{urps_demand_scalars()}}, \code{\link[=urps_demand_levers]{urps_demand_levers()}}

Other URPS demand:
\code{\link{URPS_DEMAND_SCALARS_VERSION}},
\code{\link{URPS_DEMAND_VERSION}},
\code{\link[=read_urps_demand_params]{read_urps_demand_params()}},
\code{\link[=urps_demand_fte]{urps_demand_fte()}},
\code{\link[=urps_demand_levers]{urps_demand_levers()}},
\code{\link[=urps_demand_params]{urps_demand_params()}},
\code{\link[=urps_demand_params_schema]{urps_demand_params_schema()}},
\code{\link[=urps_demand_scalar]{urps_demand_scalar()}},
\code{\link[=urps_demand_scalars]{urps_demand_scalars()}},
\code{\link[=validate_urps_demand_params]{validate_urps_demand_params()}}
}
\concept{URPS demand}
