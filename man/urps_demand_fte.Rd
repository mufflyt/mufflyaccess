% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_demand.R
\name{urps_demand_fte}
\alias{urps_demand_fte}
\title{Scenario-aware demand FTE: registry lookup + clinical FTE in one call}
\usage{
urps_demand_fte(population, visits_per_fte, scenario_id = "baseline")
}
\arguments{
\item{population}{A \code{data.frame} — see \code{\link[=urps_demand_clinical_fte]{urps_demand_clinical_fte()}}.}

\item{visits_per_fte}{Annual URPS visits per FTE provider. See
\code{\link[=urps_demand_clinical_fte]{urps_demand_clinical_fte()}}.}

\item{scenario_id}{A registered scenario id (default \code{"baseline"}). The
four demand levers are resolved from the registry; passing raw lever values
is not required.}
}
\value{
Length-1 numeric \code{demand_clinical_fte}, or \code{NA_real_} until calibrated.
}
\description{
The demand-side counterpart to \code{\link[=urps_supply_fte_sex]{urps_supply_fte_sex()}}. Looks up
the demand lever bundle for a registered scenario via \code{\link[=urps_demand_levers]{urps_demand_levers()}},
then calls \code{\link[=urps_demand_clinical_fte]{urps_demand_clinical_fte()}} — so cliff passes a \code{scenario_id}
string rather than four raw lever values:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Supply side (cliff already does this):
supply_fte <- mufflyaccess::urps_supply_fte_sex(cohort, baseline_scale,
                late_from_age = sc$late_career_fte_onset_age,
                late_factor   = sc$late_career_fte_factor)

# Demand side (new — call this once per projection row):
demand_fte <- mufflyaccess::urps_demand_fte(population, visits_per_fte,
                scenario_id = scenario_id)

# Gap (close the triangle):
gap_fte <- mufflyaccess::urps_gap_fte(supply_fte, demand_fte)
}\if{html}{\out{</div>}}

Returns \code{NA_real_} for any scenario until \code{urps_demand_params()} carries
calibrated coefficients (\code{calibration_status != "not_calibrated"}). Cliff
can call this unconditionally — the \code{NA} propagates to \code{gap_fte} and the
contract validator allows \code{NA} in optional columns.
}
\examples{
pop <- data.frame(age = 50, sex = "female", n = 10000)
urps_demand_fte(pop, visits_per_fte = 2000)                           # NA
urps_demand_fte(pop, visits_per_fte = 2000, scenario_id = "baseline") # NA
urps_demand_fte(pop, 2000, scenario_id = "demand_managed_care_increase") # NA
}
\seealso{
\code{\link[=urps_demand_clinical_fte]{urps_demand_clinical_fte()}}, \code{\link[=urps_demand_levers]{urps_demand_levers()}},
\code{\link[=urps_supply_fte_sex]{urps_supply_fte_sex()}}, \code{\link[=urps_gap_fte]{urps_gap_fte()}}

Other URPS demand:
\code{\link{URPS_DEMAND_SCALARS_VERSION}},
\code{\link{URPS_DEMAND_VERSION}},
\code{\link[=urps_demand_clinical_fte]{urps_demand_clinical_fte()}},
\code{\link[=urps_demand_levers]{urps_demand_levers()}},
\code{\link[=urps_demand_params]{urps_demand_params()}},
\code{\link[=urps_demand_scalar]{urps_demand_scalar()}},
\code{\link[=urps_demand_scalars]{urps_demand_scalars()}}
}
\concept{URPS demand}
