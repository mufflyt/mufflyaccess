% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_fte_sex.R
\name{urps_effective_fte_sex}
\alias{urps_effective_fte_sex}
\title{Effective clinical FTE of a sex-stratified headcount cohort}
\usage{
urps_effective_fte_sex(
  counts,
  scale = 1,
  late_from_age = NULL,
  late_factor = 1
)
}
\arguments{
\item{counts}{A \code{data.frame} with columns \code{age}, \code{sex}, \code{pathway}, and \code{n}.
Mixed-sex rows are handled natively (no pre-splitting required).}

\item{scale}{Normalization scale from \code{\link[=urps_fte_scale_sex]{urps_fte_scale_sex()}} (default \code{1} =
raw hours/40 weighted sum).}

\item{late_from_age, late_factor}{Passed to \code{\link[=urps_fte_weight_sex]{urps_fte_weight_sex()}}.}
}
\value{
A length-1 numeric (clinical FTE in 40-hrs/wk units after scaling).
}
\description{
Sex-aware counterpart to \code{\link[=urps_effective_fte]{urps_effective_fte()}}: computes
\code{scale * sum(n * urps_fte_weight_sex(age, sex, pathway, ...))} for a
cohort data.frame that carries per-row sex. Use \code{\link[=urps_fte_scale_sex]{urps_fte_scale_sex()}} to
anchor the reference cohort's FTE to its headcount before comparing across
years or geographies.
}
\examples{
cs <- data.frame(
  age     = c(45L, 62L, 45L, 62L),
  sex     = c("female", "female", "male", "male"),
  pathway = c("ABOG", "ABOG", "ABU", "ABU"),
  n       = c(350, 150, 100, 40))
urps_effective_fte_sex(cs, scale = urps_fte_scale_sex(cs))
}
\seealso{
\code{\link[=urps_fte_scale_sex]{urps_fte_scale_sex()}}, \code{\link[=urps_fte_weight_sex]{urps_fte_weight_sex()}}, \code{\link[=urps_effective_fte]{urps_effective_fte()}}

Other URPS FTE sex:
\code{\link{URPS_FTE_REFERENCE_HOURS_PER_WEEK}},
\code{\link{URPS_FTE_SEX_HOURS_VERSION}},
\code{\link[=urps_fte_predicted_hours]{urps_fte_predicted_hours()}},
\code{\link[=urps_fte_scale_sex]{urps_fte_scale_sex()}},
\code{\link[=urps_fte_sex_hours_params]{urps_fte_sex_hours_params()}},
\code{\link[=urps_fte_weight_sex]{urps_fte_weight_sex()}},
\code{\link[=urps_supply_fte_sex]{urps_supply_fte_sex()}}
}
\concept{URPS FTE sex}
