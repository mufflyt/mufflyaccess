% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_exit_panel.R
\name{urps_exit_counts}
\alias{urps_exit_counts}
\title{Observed URPS workforce departures by year}
\usage{
urps_exit_counts(start_year = NULL, end_year = NULL)
}
\arguments{
\item{start_year, end_year}{Optional inclusive \code{exit_year} bounds.}
}
\value{
A \code{data.frame}: \code{year}, \code{n_departed} (all workforce exits),
\code{n_retired} (backwards-compatible alias of \code{n_departed}),
\code{n_retired_with_evidence} (the \code{exit_reason == "retired"} subset),
\code{retirement_status = "observed"}, \code{retirement_definition}.
}
\description{
Aggregates \code{\link[=urps_departures]{urps_departures()}} to annual observed departure
counts -- the real numbers the workforce-count series can finally carry
instead of \code{NA}. The primary event is \strong{departure from the practicing
workforce} (\code{n_departed}); retirement is only one \emph{observed reason} for it,
so \code{n_retired_with_evidence} (the \code{exit_reason == "retired"} subset) is the
count you should use when you specifically mean retirement.
\code{n_retired} is retained as a \strong{backwards-compatible alias of \code{n_departed}}
(equal in value) for downstream contracts that still read that name; new
code should prefer \code{n_departed}. \code{retirement_definition = "observed_workforce_exit"}
records that this counts departures from practice, not only self-declared
retirements.
}
\examples{
ex <- system.file("extdata", "urps_observed_departures_example.csv",
                  package = "mufflyaccess")
old <- options(mufflyaccess.urps_departures_path = ex)
urps_exit_counts()
options(old)
}
\seealso{
\code{\link[=urps_departures]{urps_departures()}}, \code{\link[=urps_exit_hazard_by_age_year]{urps_exit_hazard_by_age_year()}}

Other URPS exit panel:
\code{\link[=urps_departures]{urps_departures()}},
\code{\link[=urps_exit_hazard_by_age_year]{urps_exit_hazard_by_age_year()}},
\code{\link[=validate_urps_exit_evidence]{validate_urps_exit_evidence()}}
}
\concept{URPS exit panel}
