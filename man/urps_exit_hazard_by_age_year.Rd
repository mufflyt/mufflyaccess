% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_exit_panel.R
\name{urps_exit_hazard_by_age_year}
\alias{urps_exit_hazard_by_age_year}
\title{Observed URPS exit hazard by age and year}
\usage{
urps_exit_hazard_by_age_year()
}
\value{
A \code{data.frame}: \code{age}, \code{year}, \code{n_at_risk}, \code{n_exits}, \code{exit_hazard}
(in [0, 1]), \code{hazard_source}.
}
\description{
Serves the frozen empirical departure-hazard artifact --
\code{n_exits / n_at_risk} per age x year from the provider-month panel's risk
sets. This is what cliff estimates its forward departure process from:
\emph{past} departures are observed here; cliff still \emph{simulates} future
departures, but calibrated to these observed hazards rather than the frozen
2016-2021 curve. Configure via \code{options(mufflyaccess.urps_exit_hazard_path=...)}
or \code{MUFFLYACCESS_URPS_EXIT_HAZARD}; fail-loud when unset.
}
\examples{
ex <- system.file("extdata", "urps_exit_hazard_by_age_year_example.csv",
                  package = "mufflyaccess")
old <- options(mufflyaccess.urps_exit_hazard_path = ex)
head(urps_exit_hazard_by_age_year())
options(old)
}
\seealso{
\code{\link[=urps_departures]{urps_departures()}}, \code{\link[=urps_exit_counts]{urps_exit_counts()}}

Other URPS exit panel:
\code{\link[=urps_departures]{urps_departures()}},
\code{\link[=urps_exit_counts]{urps_exit_counts()}},
\code{\link[=validate_urps_exit_evidence]{validate_urps_exit_evidence()}}
}
\concept{URPS exit panel}
