% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_fte_sex.R
\name{urps_fte_predicted_hours}
\alias{urps_fte_predicted_hours}
\title{Predicted weekly patient care hours by age and sex}
\usage{
urps_fte_predicted_hours(age, sex)
}
\arguments{
\item{age}{Numeric or integer vector of ages. Must be non-empty.}

\item{sex}{Character vector of \code{"female"} / \code{"male"} values, recycled against
\code{age}. Can be length-1 (applied to all ages) or the same length as \code{age}
(one sex per provider row).}
}
\value{
Numeric vector the same length as \code{max(length(age), length(sex))}
(weekly hours; may be negative at extreme ages).
}
\description{
Evaluates the quadratic OLS model
\code{hours(age) = intercept + b_age * age + c_age_sq * age^2}.
Vectorized over both \code{age} and \code{sex} (recycled against each other, so a
mixed-sex cohort vector works directly). Raw (unclamped) predictions can go
negative at extreme ages; use \code{\link[=urps_fte_weight_sex]{urps_fte_weight_sex()}} for FTE calculations,
which clamps to zero.
}
\examples{
urps_fte_predicted_hours(35:75, "female")
urps_fte_predicted_hours(47, "male")          # peak: ~55 hrs/week
# mixed-sex cohort vector:
urps_fte_predicted_hours(c(45, 55), c("female", "male"))
}
\seealso{
\code{\link[=urps_fte_weight_sex]{urps_fte_weight_sex()}}, \code{\link[=urps_fte_sex_hours_params]{urps_fte_sex_hours_params()}}

Other URPS FTE sex:
\code{\link{URPS_FTE_REFERENCE_HOURS_PER_WEEK}},
\code{\link{URPS_FTE_SEX_HOURS_VERSION}},
\code{\link[=urps_effective_fte_sex]{urps_effective_fte_sex()}},
\code{\link[=urps_fte_scale_sex]{urps_fte_scale_sex()}},
\code{\link[=urps_fte_sex_hours_params]{urps_fte_sex_hours_params()}},
\code{\link[=urps_fte_weight_sex]{urps_fte_weight_sex()}},
\code{\link[=urps_supply_fte_sex]{urps_supply_fte_sex()}}
}
\concept{URPS FTE sex}
