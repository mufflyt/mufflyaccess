% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_fte_sex.R
\name{urps_fte_scale_sex}
\alias{urps_fte_scale_sex}
\title{Normalization scale anchoring a sex-stratified reference cohort's FTE to a target}
\usage{
urps_fte_scale_sex(
  reference_counts,
  target_headcount = sum(reference_counts$n)
)
}
\arguments{
\item{reference_counts}{A \code{data.frame(age, sex, pathway, n)} — typically the
2023 baseline active cohort with sex distribution applied.}

\item{target_headcount}{The headcount the reference cohort's FTE should equal
(default \code{sum(reference_counts$n)}, i.e. FTE(reference) == headcount).}
}
\value{
A length-1 numeric scale for \code{\link[=urps_effective_fte_sex]{urps_effective_fte_sex()}}.
}
\description{
Sex-aware counterpart to \code{\link[=urps_fte_scale]{urps_fte_scale()}}: computes the scale
factor so that \code{urps_effective_fte_sex(reference_counts, scale)} equals
\code{target_headcount}. Apply once to the 2023 baseline cohort; reuse the same
scale for all projection years so FTE is comparable across time.
}
\examples{
cs <- data.frame(
  age     = c(45L, 62L, 45L, 62L),
  sex     = c("female", "female", "male", "male"),
  pathway = c("ABOG", "ABOG", "ABU", "ABU"),
  n       = c(350, 150, 100, 40)
)
urps_fte_scale_sex(cs)
}
\seealso{
\code{\link[=urps_effective_fte_sex]{urps_effective_fte_sex()}}, \code{\link[=urps_fte_scale]{urps_fte_scale()}}

Other URPS FTE sex:
\code{\link{URPS_FTE_REFERENCE_HOURS_PER_WEEK}},
\code{\link{URPS_FTE_SEX_HOURS_VERSION}},
\code{\link[=urps_effective_fte_sex]{urps_effective_fte_sex()}},
\code{\link[=urps_fte_predicted_hours]{urps_fte_predicted_hours()}},
\code{\link[=urps_fte_sex_hours_params]{urps_fte_sex_hours_params()}},
\code{\link[=urps_fte_weight_sex]{urps_fte_weight_sex()}},
\code{\link[=urps_supply_fte_sex]{urps_supply_fte_sex()}}
}
\concept{URPS FTE sex}
