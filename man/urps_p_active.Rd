% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_lfp.R
\name{urps_p_active}
\alias{urps_p_active}
\title{P(actively practicing) given age and sex}
\usage{
urps_p_active(age, sex)
}
\arguments{
\item{age}{Numeric or integer vector of ages. Must be non-empty.}

\item{sex}{Character vector of \code{"female"} / \code{"male"} values, recycled
against \code{age}.}
}
\value{
Numeric vector in (0, 1) the same length as \code{max(length(age), length(sex))}.
}
\description{
The logistic labor force participation probability:
\code{P(active | age, sex) = 1 / (1 + exp(-(intercept + b_age * age)))}.
Captures sabbaticals, leaves of absence, and part-time transitions that
do not appear as permanent retirements in the certification record.
Vectorized over both \code{age} and \code{sex}.
}
\details{
\strong{How cliff uses this:} multiply the certified (not permanently
retired) headcount by \code{urps_p_active()} to get the practicing count before
applying the FTE weight:

\if{html}{\out{<div class="sourceCode">}}\preformatted{practicing_n    <- certified_n * urps_p_active(age, sex)
supply_fte_cell <- practicing_n * urps_fte_weight_sex(age, sex, pathway, ...)
}\if{html}{\out{</div>}}
}
\examples{
urps_p_active(40, "female")   # ~0.97
urps_p_active(65, "male")     # ~0.85
urps_p_active(35:75, "female")
# mixed-sex cohort:
urps_p_active(c(45, 50), c("female", "male"))
}
\seealso{
\code{\link[=urps_lfp_params]{urps_lfp_params()}}, \code{\link[=urps_lfp_curve]{urps_lfp_curve()}}, \code{\link[=urps_retirement_hazard]{urps_retirement_hazard()}},
\code{\link[=urps_fte_weight_sex]{urps_fte_weight_sex()}}

Other URPS LFP:
\code{\link{URPS_LFP_VERSION}},
\code{\link[=urps_apply_lfp]{urps_apply_lfp()}},
\code{\link[=urps_lfp_curve]{urps_lfp_curve()}},
\code{\link[=urps_lfp_params]{urps_lfp_params()}}
}
\concept{URPS LFP}
