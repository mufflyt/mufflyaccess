% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_retirement.R
\name{urps_p_still_active}
\alias{urps_p_still_active}
\title{P(still active) at a given age under the URPS logistic retirement model}
\usage{
urps_p_still_active(age, sex, pathway, retirement_shift_years = 0L)
}
\arguments{
\item{age}{Numeric or integer vector of ages to evaluate. Must be non-empty.}

\item{sex}{\code{"female"} or \code{"male"} (length-1).}

\item{pathway}{\code{"ABOG"} or \code{"ABU"} (per-individual pathway; length-1).}

\item{retirement_shift_years}{Finite numeric scalar from the scenario
registry (\code{urps_scenario(id)$retirement_shift_years}; default \code{0L}).}
}
\value{
Numeric vector the same length as \code{age}, values in (0, 1).
}
\description{
The logistic survival function
\verb{S(age; mu, sigma) = 1 / (1 + exp((age - mu) / sigma))},
evaluated at the effective age after applying the scenario retirement shift.
Vectorized over \code{age}; \code{sex} and \code{pathway} must be length-1 scalars (call
once per sex x pathway cell of your projection cohort).
}
\details{
\strong{Scenario shift:} \code{effective_age = age - retirement_shift_years}.
Pass \code{urps_scenario(id)$retirement_shift_years} directly.
\itemize{
\item \code{retirement_shift_years = -2} (retire earlier): \code{effective_age = age + 2},
so the curve shifts left — lower survival — more exits. ✓
\item \code{retirement_shift_years = +2} (retire later): \code{effective_age = age - 2},
higher survival — fewer exits. ✓
}
}
\examples{
# at mu (65), female ABOG survival is exactly 0.5
urps_p_still_active(65, "female", "ABOG")
# full age range, no scenario shift
urps_p_still_active(35:80, "male", "ABU")
# retire-2-years-earlier scenario
urps_p_still_active(65, "female", "ABOG",
  retirement_shift_years = urps_scenario("retire_2yr_earlier")$retirement_shift_years)
}
\seealso{
\code{\link[=urps_retirement_hazard]{urps_retirement_hazard()}}, \code{\link[=urps_survival_curve]{urps_survival_curve()}},
\code{\link[=urps_retirement_params]{urps_retirement_params()}}, \code{\link[=urps_scenarios]{urps_scenarios()}}

Other URPS retirement curve:
\code{\link{URPS_RETIREMENT_CURVE_VERSION}},
\code{\link[=urps_retirement_hazard]{urps_retirement_hazard()}},
\code{\link[=urps_retirement_params]{urps_retirement_params()}},
\code{\link[=urps_survival_curve]{urps_survival_curve()}}
}
\concept{URPS retirement curve}
