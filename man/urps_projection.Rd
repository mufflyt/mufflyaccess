% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_projection.R
\name{urps_projection}
\alias{urps_projection}
\title{The canonical URPS supply projection}
\usage{
urps_projection(
  scenario = "baseline",
  geography = "national",
  pathway = "ABOG_PLUS_ABU"
)
}
\arguments{
\item{scenario}{Scenario id; only \code{"baseline"} is currently published.}

\item{geography}{\code{"national"} (default) or \code{"conus"}.}

\item{pathway}{Board pathway; only \code{"ABOG_PLUS_ABU"} is currently published.}
}
\value{
A one-row \code{data.frame}. Key columns: \code{baseline_year},
\code{baseline_headcount}, \code{horizon_year}, \code{projected_headcount},
\code{projected_headcount_ramped}, \code{lower_95}, \code{upper_95}, \code{annual_entrants},
\code{mean_annual_exits}, \code{replacement_ratio}.
}
\description{
The published URPS projection -- baseline headcount, horizon
headcount with and without the entry ramp, interval, entrants, exits and the
replacement ratio -- served from the bundled artifact so consumers stop
copying the producing repository's CSVs.
}
\details{
This is the reviewed RESULT of cliff's projection, not a re-run of it.
Use \code{\link[=read_urps_projection]{read_urps_projection()}} and \code{\link[=validate_urps_projection]{validate_urps_projection()}} when you have
your own projection table to check against the contract; use this when you
want the published numbers.

\code{replacement_ratio} is \code{annual_entrants / mean_annual_exits}, and
\code{baseline_headcount} is checked against \code{\link[=urps_count]{urps_count()}} on every call, so the
projection can never start from a number the SSOT does not serve.

\code{projected_headcount} is the immediate-entry projection. The entry-ramped
variant (\code{projected_headcount_ramped}) defers new entrants over the observed
certification-to-practice curve and is reported as a sensitivity, not the
headline.
}
\examples{
p <- urps_projection()
p$baseline_headcount
p$replacement_ratio
}
\seealso{
\code{\link[=urps_active_ages]{urps_active_ages()}} for the cohort it projects,
\code{\link[=urps_count]{urps_count()}} for the baseline it must agree with,
\code{\link[=validate_urps_projection]{validate_urps_projection()}} for checking your own table.

Other URPS SSOT:
\code{\link[=urps_active_ages]{urps_active_ages()}}
}
\concept{URPS SSOT}
