% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_projection_ci.R
\name{urps_projection_ci}
\alias{urps_projection_ci}
\title{Parametric-bootstrap confidence intervals for the URPS supply projection}
\usage{
urps_projection_ci(
  project_fn,
  scenarios = "baseline",
  years = 2025:2045,
  B = 200,
  seed = 42,
  retirement_sigma_sd = 0.51,
  entrant_cv = 0.077,
  probs = c(0.025, 0.975)
)
}
\arguments{
\item{project_fn}{A \verb{function(scenario_id, years, param_draw)} that returns a
\code{data.frame} containing at minimum the columns \code{year} (integer),
\code{scenario_id} (character), \code{supply_headcount} (numeric), and
\code{supply_clinical_fte} (numeric, may be \code{NA}). The \code{param_draw} argument is
the list returned by \code{\link[=urps_ci_param_draw]{urps_ci_param_draw()}}; the function applies the
perturbations to its recurrence engine as appropriate.}

\item{scenarios}{Character vector of registered scenario ids to bootstrap.
Each must pass \code{\link[=is_urps_scenario]{is_urps_scenario()}}. Default \code{"baseline"}.}

\item{years}{Integer vector of projection years. Default \code{2025:2045}.}

\item{B}{Number of bootstrap replicates. Must be an integer \verb{>= 10}.
Default \code{200}.}

\item{seed}{Single integer RNG seed for reproducibility. Default \code{42}.}

\item{retirement_sigma_sd}{Passed to \code{\link[=urps_ci_param_draw]{urps_ci_param_draw()}}. Default \code{0.51}.}

\item{entrant_cv}{Passed to \code{\link[=urps_ci_param_draw]{urps_ci_param_draw()}}. Default \code{0.077}.}

\item{probs}{Length-2 numeric giving the lower and upper quantile
probabilities. Both must be in (0, 1) and \code{probs[1] < probs[2]}. Default
\code{c(0.025, 0.975)}.}
}
\value{
A \code{data.frame} with one row per \verb{(year, scenario_id)} combination and
columns:
\describe{
\item{year}{integer projection year}
\item{scenario_id}{character scenario identifier}
\item{lower_headcount_95}{lower quantile of \code{supply_headcount} across
replicates (at \code{probs[1]})}
\item{upper_headcount_95}{upper quantile of \code{supply_headcount} across
replicates (at \code{probs[2]})}
\item{lower_fte_95}{lower quantile of \code{supply_clinical_fte} across
replicates (at \code{probs[1]}); \code{NA} if all replicate FTE values are \code{NA}}
\item{upper_fte_95}{upper quantile of \code{supply_clinical_fte} across
replicates (at \code{probs[2]}); \code{NA} if all replicate FTE values are \code{NA}}
}
}
\description{
Runs \code{B} parametric-bootstrap replicates of a user-supplied URPS
projection function and returns per-year, per-scenario quantile bounds on
\code{supply_headcount} and \code{supply_clinical_fte}.
}
\details{
\strong{Three uncertainty sources} are modelled as independent normal draws
applied per replicate (HWMM uncertainty modelling convention):
\itemize{
\item \strong{Retirement timing (±1 yr at 95\%):} a normal shift
(\code{sd = retirement_sigma_sd = 0.51}) is added to the retirement-curve mu
for every sex × pathway cell. This reflects uncertainty in the median
retirement age derived from literature rather than ABOG-specific lapse
records.
\item \strong{Entrant count (±15\% at 95\%):} a multiplicative normal scale
(\verb{mean = 1, cv = entrant_cv = 0.077}) is applied to the annual entrant
count. This reflects year-to-year fellowship-output uncertainty and
programme-capacity estimation error.
\item \strong{LFP intercept (additive, sd = 0.05):} a normal shift is added to
the logistic LFP intercept for every sex cell, reflecting uncertainty in
the anchor-point participation rates from the 2021 ACOG and 2022 AMA
surveys.
}
The three draws are statistically independent within each replicate. The
caller's \code{project_fn} is responsible for applying them; \code{\link[=urps_ci_param_draw]{urps_ci_param_draw()}}
documents the list contract.
}
\examples{
\dontrun{
my_project_fn <- function(scenario_id, years, param_draw) {
  # apply param_draw perturbations inside your projection engine
  data.frame(
    year = years,
    scenario_id = scenario_id,
    supply_headcount = seq(1300, 1200, length.out = length(years)) *
      param_draw$entrant_scale,
    supply_clinical_fte = NA_real_
  )
}
ci <- urps_projection_ci(my_project_fn,
  scenarios = "baseline",
  years = 2025:2035, B = 50, seed = 1L
)
head(ci)
}
}
\seealso{
\code{\link[=urps_ci_param_draw]{urps_ci_param_draw()}}, \code{\link[=urps_projection_schema]{urps_projection_schema()}}

Other URPS projection CI:
\code{\link[=urps_ci_param_draw]{urps_ci_param_draw()}}
}
\concept{URPS projection CI}
