% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_count.R
\name{urps_provenance}
\alias{urps_provenance}
\title{Provenance / manifest for the URPS workforce SSOT}
\usage{
urps_provenance(detailed = FALSE)
}
\arguments{
\item{detailed}{If \code{TRUE}, add the nested \code{detail} element (see \strong{Value}) with
the full source-to-artifact provenance chain and a live integrity check.
Default \code{FALSE} returns the stable summary only.}
}
\value{
A named list:
\describe{
\item{artifact_version, contract_version}{version strings (e.g. \code{"3.0.0"})}
\item{artifact_source}{\code{"external"} or \code{"bundled_bootstrap"}}
\item{canonical_release, suitable_for_release}{release-readiness flags}
\item{canonical_2023_estimand}{human-readable canonical-cell statement}
\item{external_artifact_error}{\code{NULL}, or the fallback reason}
\item{measure_years}{integer vector (2013:2023)}
\item{measures, geographies, boards}{the published dimensions}
\item{snapshot_date}{a \code{Date}; distinct from the measure year}
\item{roster_reflects_certifications_through}{last cert year in the roster}
\item{geographic_scope, active_in_year_definition, deduplication_rule}{
definitional metadata}
\item{source_sha256, source_git_commit, git_commit_semantics}{source
integrity + commit provenance}
\item{source_description, source_systems}{disclosure-ready source wording}
\item{retired_cells}{cells retired by an earlier contract (see
\code{\link[=urps_retired_values]{urps_retired_values()}})}
\item{method_version, package_version}{producer + installed package versions}
}
With \code{detailed = TRUE}, one extra element \code{detail} -- a nested list carrying
the full, verifiable provenance chain:
\describe{
\item{artifact_dir, created_at}{served directory and artifact build time}
\item{measure_year, model_baseline_year, roster_snapshot_date}{the three
distinct years, kept separate}
\item{geography_resolution_rule, state_source_counts}{how each provider's
state (hence CONUS membership) was resolved, and from which source}
\item{cohort_definition}{the URPS-subspecialty-cert basis: real cert dates,
the fellowship-proxy fallbacks, and how many providers used each}
\item{provider_snapshot}{grain and the reconstruction counts
(\code{rows_national}, \code{rows_active_2023}, future certifications)}
\item{source_files}{a \code{data.frame(name, path, sha256)} of the enriched
source rosters}
\item{combined_source_sha256, output_files}{the combined source hash and
the produced-artifact hashes}
\item{integrity}{a \strong{live} check -- the served CSV / parquet SHA-256
recomputed and compared to the manifest (\verb{*_verified} is \code{TRUE}/\code{FALSE},
or \code{NA} when \code{digest} is unavailable)}
\item{known_limitations}{the producer's documented caveats}
}
}
\description{
Everything needed to cite or audit the served numbers: contract /
artifact versions, the measures and geographies, the snapshot date, the
active-in-year and deduplication definitions, the source hash and git commit,
the disclosure-ready source wording, release-readiness flags, and the recorded
retired cells.
}
\details{
\code{artifact_source} is \code{"external"} when a released artifact is served
and \code{"bundled_bootstrap"} otherwise -- including after a silent option/env
fallback, whose reason is then in \code{external_artifact_error}.
\code{canonical_release} / \code{suitable_for_release} are \code{FALSE} for the bootstrap.
Accessing this on an unusable external source emits a warning (see
\code{\link[=use_urps_artifact]{use_urps_artifact()}}).
}
\examples{
urps_provenance()$canonical_2023_estimand
urps_provenance()[c("contract_version", "artifact_source", "canonical_release")]

# the full chain, with a live SHA-256 integrity check of the served bytes:
d <- urps_provenance(detailed = TRUE)$detail
d$source_files # enriched rosters + hashes
d$cohort_definition$source_counts # how the active cohort was dated
d$integrity$counts_csv_verified # TRUE: served bytes match the manifest
}
\seealso{
\code{\link[=urps_count]{urps_count()}}, \code{\link[=urps_lineage]{urps_lineage()}}, \code{\link[=validate_urps_artifact]{validate_urps_artifact()}},
\code{\link[=validate_urps_ssot]{validate_urps_ssot()}}, \code{\link[=use_urps_artifact]{use_urps_artifact()}}

Other URPS workforce:
\code{\link[=compare_urps_artifacts]{compare_urps_artifacts()}},
\code{\link[=urps_count]{urps_count()}},
\code{\link[=urps_counts]{urps_counts()}},
\code{\link[=urps_counts_long]{urps_counts_long()}},
\code{\link[=urps_entrants]{urps_entrants()}},
\code{\link[=urps_entry_counts]{urps_entry_counts()}},
\code{\link[=urps_lineage]{urps_lineage()}},
\code{\link[=urps_require_retirement_ascertained]{urps_require_retirement_ascertained()}},
\code{\link[=urps_retired_values]{urps_retired_values()}},
\code{\link[=urps_retirement_status]{urps_retirement_status()}},
\code{\link[=use_urps_artifact]{use_urps_artifact()}},
\code{\link[=validate_urps_artifact]{validate_urps_artifact()}},
\code{\link[=validate_urps_ssot]{validate_urps_ssot()}}
}
\concept{URPS workforce}
