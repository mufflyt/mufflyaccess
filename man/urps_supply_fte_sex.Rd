% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_fte_sex.R
\name{urps_supply_fte_sex}
\alias{urps_supply_fte_sex}
\title{Full supply pipeline: certified headcount -> supply_clinical_fte (sex-stratified)}
\usage{
urps_supply_fte_sex(
  cohort,
  baseline_scale,
  late_from_age = NULL,
  late_factor = 1
)
}
\arguments{
\item{cohort}{A \code{data.frame} with columns \code{age}, \code{sex}, \code{pathway}, and
\code{certified_n}. The \code{n} column (if present) is ignored; \code{certified_n} is used.}

\item{baseline_scale}{Scale from \code{\link[=urps_fte_scale_sex]{urps_fte_scale_sex()}} computed on the 2023
baseline certified cohort. Must be computed ONCE and reused across years.}

\item{late_from_age, late_factor}{Late-career FTE lever — pass values from
\code{urps_scenario(id)$late_career_fte_onset_age} and
\code{urps_scenario(id)$late_career_fte_factor}. Defaults leave weight unchanged.}
}
\value{
Length-1 numeric: \code{supply_clinical_fte} for this cohort row.
}
\description{
Composes the three supply-side adjustments in the correct order:
\enumerate{
\item \strong{LFP} (\code{urps_p_active}): \verb{certified_n → practicing_n}
\item \strong{FTE weight} (\code{urps_fte_weight_sex}): hours/40 × pathway clinical time × late_factor
\item \strong{Scale} (\code{baseline_scale}): anchors FTE to the 2023 baseline headcount
}

Equivalent to:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cohort <- urps_apply_lfp(cohort)   # adds practicing_n
urps_effective_fte_sex(
  data.frame(age=cohort$age, sex=cohort$sex,
             pathway=cohort$pathway, n=cohort$practicing_n),
  scale = baseline_scale, ...)
}\if{html}{\out{</div>}}

Use this function when cliff passes certified headcount per cohort cell.
If practicing headcount is already known (e.g. from survey direct-count),
call \code{\link[=urps_effective_fte_sex]{urps_effective_fte_sex()}} directly.
}
\examples{
cohort <- data.frame(
  age = c(45L, 62L, 45L, 62L),
  sex = c("female", "female", "male", "male"),
  pathway = c("ABOG", "ABOG", "ABU", "ABU"),
  certified_n = c(350, 150, 100, 40)
)
scale <- urps_fte_scale_sex(
  cbind(cohort, n = cohort$certified_n *
    urps_p_active(cohort$age, cohort$sex))
)
urps_supply_fte_sex(cohort, scale)
}
\seealso{
\code{\link[=urps_apply_lfp]{urps_apply_lfp()}}, \code{\link[=urps_effective_fte_sex]{urps_effective_fte_sex()}}, \code{\link[=urps_fte_scale_sex]{urps_fte_scale_sex()}},
\code{\link[=urps_p_active]{urps_p_active()}}, \code{\link[=urps_fte_weight_sex]{urps_fte_weight_sex()}}

Other URPS FTE sex:
\code{\link{URPS_FTE_REFERENCE_HOURS_PER_WEEK}},
\code{\link{URPS_FTE_SEX_HOURS_VERSION}},
\code{\link[=urps_effective_fte_sex]{urps_effective_fte_sex()}},
\code{\link[=urps_fte_predicted_hours]{urps_fte_predicted_hours()}},
\code{\link[=urps_fte_scale_sex]{urps_fte_scale_sex()}},
\code{\link[=urps_fte_sex_hours_params]{urps_fte_sex_hours_params()}},
\code{\link[=urps_fte_weight_sex]{urps_fte_weight_sex()}}
}
\concept{URPS FTE sex}
