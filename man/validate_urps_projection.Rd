% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urps_projection.R
\name{validate_urps_projection}
\alias{validate_urps_projection}
\title{Validate a URPS projection table against the contract (fail loud)}
\usage{
validate_urps_projection(x, baseline_tie = NULL, tol = 1e-06)
}
\arguments{
\item{x}{A projection \code{data.frame}, or a path to a CSV to read
(\code{\link[=read_urps_projection]{read_urps_projection()}} is used).}

\item{baseline_tie}{Optional named list pinning the starting stock to the served
count: \code{list(year=, measure=, geography_type=, certification_pathway=)}. The
pathway must be \code{"ABOG"} or \code{"ABOG_PLUS_ABU"} (the two \code{\link[=urps_count]{urps_count()}} exposes).}

\item{tol}{Numeric tolerance for the \code{net_change == entrants - exits} identity
(default \code{1e-6}).}
}
\value{
Invisibly \code{TRUE}; otherwise stops with the failed check.
}
\description{
Checks that a cliff-produced projection table conforms to the
projection contract, failing loud with a specific message on the first
violation. Verifies far more than column presence: every \code{scenario_id} is
registered in \code{\link[=urps_scenarios]{urps_scenarios()}}, the \code{baseline} scenario is present, the
\code{certification_pathway} / \code{geography_type} values are the count-contract
vocabularies, there are no duplicate series keys, the 95\% bounds bracket the
point estimate, counts are non-negative, and \code{net_change} reconciles as
\code{entrants - exits}. Optionally ties the baseline-year starting stock back to
\code{\link[=urps_count]{urps_count()}} so a projection can never start from a number the SSOT does not
serve.
}
\details{
Checks performed, each failing loud:
\itemize{
\item all required (non-optional) columns present;
\item \code{scenario_id} values all registered (\code{\link[=validate_urps_scenarios]{validate_urps_scenarios()}}) and
the \code{baseline} scenario present;
\item \code{certification_pathway} in ABOG / ABU_NET_NEW / ABOG_PLUS_ABU and
\code{geography_type} in national / conus;
\item no duplicate \verb{(year, scenario_id, specialty, certification_pathway, geography_type, geography_id)} key;
\item \code{supply_headcount} non-negative; where present, \code{entrants} / \code{exits}
non-negative and \verb{lower_95 <= supply_headcount <= upper_95};
\item where all three are present, \code{net_change == entrants - exits} (within
\code{tol});
\item with \code{baseline_tie}, the \code{baseline}-scenario \code{supply_headcount} at the
named year/geography/pathway equals \code{\link[=urps_count]{urps_count()}} for the matching
measure.
}
}
\examples{
p <- read_urps_projection(system.file(
  "extdata", "urps_projection_example.csv", package = "mufflyaccess"))
validate_urps_projection(p)
# tie the 2025 baseline stock to the served roster snapshot (1339):
validate_urps_projection(p, baseline_tie = list(
  year = 2025, measure = "roster_snapshot",
  geography_type = "national", certification_pathway = "ABOG_PLUS_ABU"))
}
\seealso{
\code{\link[=urps_projection_schema]{urps_projection_schema()}}, \code{\link[=read_urps_projection]{read_urps_projection()}},
\code{\link[=validate_urps_scenarios]{validate_urps_scenarios()}}, \code{\link[=urps_count]{urps_count()}}

Other URPS projection: 
\code{\link{URPS_PROJECTION_CONTRACT_VERSION}},
\code{\link{read_urps_projection}()},
\code{\link{urps_projection_schema}()}
}
\concept{URPS projection}
