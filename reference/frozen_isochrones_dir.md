# Path to the verified frozen isochrone set

Path to the verified frozen isochrone set

## Usage

``` r
frozen_isochrones_dir(verify = TRUE)
```

## Arguments

- verify:

  Re-hash before returning (default `TRUE`).

## Value

Directory path. Errors if no verified set can be resolved.
