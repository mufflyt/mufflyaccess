# Adopt a directory as the frozen isochrone SSOT

Verifies before adopting, so an invalid directory can never become the
source.

## Usage

``` r
use_frozen_isochrones(dir)
```

## Arguments

- dir:

  Directory holding the four consolidated isochrone files.

## Value

The adopted path, invisibly.
